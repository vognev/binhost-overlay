#!/bin/bash

set -e

CHOST="${CHOST:-x86_64-pc-linux-gnu}"

# todo: depclean all except bdeps of $@

"${CHOST}-emerge" -v1 --onlydeps --with-bdeps=y "$@"

FEATURES="-gtbinpkg" \
    "${CHOST}-emerge" -v1 --buildpkgonly --usepkg=n "$@"
